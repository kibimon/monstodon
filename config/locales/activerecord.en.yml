---
en:
  activerecord:
    errors:
      models:
        account:
          attributes:
            base:
              multiple: cannot have multiple types
              none: must have a type
            username:
              invalid: only letters, numbers and underscores
        status:
          attributes:
            reblog:
              taken: of status already exists
